EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# No map tokens needed — MapLibre uses a local Liberty style from assets/map-style.json.
# Map tokens are defined entirely in that file, not in environment variables.
